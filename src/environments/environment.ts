export const environment = {
  production: true,
  //Url de produccion del backend, alojada en render, este servidor se duerme cada 15 minutos por inactividad,
  //por lo que la primera vez que se accede puede tardar unos segundos en responder
  apiBaseUrl: 'https://platos-tipicos-api.onrender.com/api',
};
